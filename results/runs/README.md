# 运行记录

每次运行建立 `<YYYYMMDD>-<任务编号>-<短名称>/` 子目录，保存实际配置、环境、日志、原始输出和探索图；重算加后缀，不覆盖旧结果。具体字段和运行索引统一维护在[结果说明](../README.md)。

实际运行文件默认被 Git 忽略，重要结果需另行备份；当前备份尚未配置。输入配置源文件放 `code/configs/` 并提交 Git，避免全部复现参数只存在被忽略的目录中。
