# 论文候选图

这里只放经过核对的图。图的文件名或附带说明应能指向生成脚本、输入和关键参数。

目前尚无最终图。新图采用稳定的含义名称，例如 `dicke-baseline.pdf`，避免 `final2-new` 一类名称；探索版本留在对应运行目录。

每张图在本页登记：`文件名／回答的问题／输入运行或汇总编号／生成命令和代码版本／模型与参数／误差和收敛证据／允许支持的结论`。保存适合 LaTeX 的 PDF，必要时同时给 PNG 预览；从 LaTeX 引用这里的文件，避免复制多份后更新不同步。
