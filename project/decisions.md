# 决策记录

当题目、模型、方法或目录约定发生实质变化时，按以下格式追加记录。

## 模板

- 日期：YYYY-MM-DD
- 决定：
- 原因与证据：
- 考虑过的其他方案：
- 对代码、数据或论文的影响：

## 2026-09-19：暂以受约束的 Dicke–Ising 可达性为毕设研究候选

- 决定：先以最近邻 Ising 自旋相互作用、静态单模 `A²-ξ(a+a†)²` 为可复现的理论基线；把问题集中在物理参数约束、反压缩阈值和可达相区。此为**待与导师确认的工作假设**。
- 原因与证据：同一正交分量上的二次项可精确压缩映射；Langheld 等 2025 的 QMC 补充材料已讨论 `A²` 的可达相图，Chen 等 2021 已有无自旋相互作用的反压缩恢复。详见 [研究评估](research-assessment.md)。
- 考虑过的其他方案：直接添加反压缩并扫描 Ising/XXZ 相图，文献重叠高；多模/不对易正交分量耦合可能更有新颖性，但需另做微观与开放系统调研，当前先不作为主线。
- 对代码、数据或论文的影响：先复现 `J=0` 和 `ξ=0` 两个基准，再做扩展；论文区分静态有效基态、受驱动稳态与真实平衡。

## 2026-09-19：平衡态优先，准备阶段使用中文 LaTeX 笔记

- 决定：以静态有效模型的零温基态为主线候选，驱动耗散问题保留为有具体平台支撑时的备选；先用 `thesis/notes/main.tex` 写调研笔记，正式写作后再迁移至 APS REVTeX/PRL 风格。
- 原因与证据：符合用户明确偏好，且当前平衡模型可先完成稳定性、精确映射和相图基准；受泵浦反压缩属于不同的物理设置，不能混写。
- 考虑过的其他方案：现在直接进入开放系统或直接套 PRL 版式；前者扩大模型范围，后者不利于准备阶段反复整理中文文献与推导。
- 对代码、数据或论文的影响：统一在 `literature/references.bib` 管理引文键，在 `literature/reading-list.md` 记录阅读状态；笔记的引用可迁移到正式论文。

## 2026-09-19：平衡态研究不限于零温

- 决定：零温基态用于模型和数值基线；有限温度热平衡可作为毕设主线，待选定具体自旋模型和可检验问题。
- 原因与证据：用户明确希望考虑非零温。Peng 等（2019）讨论 Dicke 类模型的有限温度相变；Otake 与 Bamba（2026）给出腔耦合经典一维 Ising 链的有限温度精确先例。
- 研究边界：静态压缩映射在任意温度依然精确；有限温度不能单独构成新颖性。一级相界需比较自由能，有限系统的平滑变化不能直接叫作热相变。
- 对论文的影响：更新选题建议、文献库和 LaTeX 笔记的研究范围。

## 2026-09-19：先完成零温可信基线，再决定有限温度延伸

- 决定：把详细路线按“模型与平台—解析极限—小系统 ED—零温相界—研究价值筛选—条件延伸”执行；有限温度仍在课题范围内，但先要求提出超出现有量子 Ising–腔热相图的具体问题。此工作顺序待与导师核对。
- 原因与证据：Gammelmark–Mølmer（2011）已有量子 Ising–腔有限温度自由能和一、二阶相界；Leibig 等（2026，预印本）已有自洽自旋模型的精细零温相图。仅增加温度维度或使用自洽方法不足以构成新颖性。
- 考虑过的其他方案：立即并行推进 XXZ、有限温度和耗散；当前模型、计算量及物理平台尚未固定，不宜并行投入。
- 对代码、数据或论文的影响：具体阶段产物、计算记录及停走标准见[执行路线](roadmap.md)；此前“有限温度可成为主线”的范围判断仍有效，但不再把它当作默认优先顺序。

## 2026-09-20：用统一入口管理启动任务和中间产物

- 决定：项目首页展示实际构建状态；进度集中在 `progress.md`，文献解释仍集中在一份综述。模型与推导分别形成两份持续更新的文档。
- 原因与证据：用户要求清楚掌握如何启动、产出位置和重点阅读；分散的任务卡片与重复文件不利于长期维护。
- 对代码和数据的影响：外部输入、单次运行、跨运行汇总与最终图分别放在 `data/raw/`、`results/runs/`、`data/processed/` 和 `results/figures/`；首次实现运行入口时建立受 Git 管理的 `code/configs/`，运行目录另存实际配置快照。
- 保留边界：当前无数值结果；数据目录受 Git 忽略，独立备份尚未落实。后续大计算开始前需在数据/结果索引记录备份位置。

## 2026-09-20：本机项目 Python 环境与新包缓存放在 D 盘

- 决定：使用 `D:\codex\quantum-optics-env` 作为本机项目 Conda 环境，后续项目依赖的包缓存设在 `D:\codex\quantum-optics-conda-pkgs`；执行 Conda 安装时显式指定环境前缀和缓存路径。
- 原因与证据：用户希望减少 C 盘占用；项目专用环境原在 C 盘约 161 MB。D 盘 Anaconda 安装目录的 `envs/` 对当前账户只读，而 `D:\codex` 可写；新环境已与原环境核对包清单并执行 Python 验证。
- 对代码和协作的影响：更新 `code/README.md` 与 `AGENTS.md` 的解释器路径；`code/environment.yml` 保留可重建依赖，不在其中硬编码环境名，以免默认位置回到 C 盘。Conda/Codex 的全局共享缓存不当作本项目文件迁移。
